import 'package:flutter/material.dart';

class DepositoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Deposito")),
      body: Center(child: Text("Halaman Deposito")),
    );
  }
}
